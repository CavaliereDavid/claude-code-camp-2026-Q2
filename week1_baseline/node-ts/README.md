# Boukensha monorepo setup

## Instructions going forward

Use workspace: in the package.json of a specific package if they have dependencies related to it. I am leveraging pnpm so my porting won't adhere to the same structure as the ruby one.
When migrating from ruby to node-ts look for ## Considerations in the README.md, it could contain useful information.